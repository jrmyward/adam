module ApplicationCable
  # The channel provides the basic structure of grouping behavior into logical
  # units when communicating over the WebSocket connection.
  class Channel < ActionCable::Channel::Base
  end
end
