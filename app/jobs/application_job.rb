# ApplicationJob is the single place to apply all kind of customizations
# and extensions needed for an application, instead of monkey-patching
# ActiveJob::Base
class ApplicationJob < ActiveJob::Base
end
